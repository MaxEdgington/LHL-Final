import { createTheme } from '@mui/material';

// theme styles here
export const theme = createTheme({
  palette: {
    primary: {
      light: '#e4e1df',
      main: '#dedad7',
      dark: '#9b9896',
      contrastText: '#fff',
    },
    secondary: {
      light: '#e53637',
      main: '#df0405',
      dark: '#9c0203',
      contrastText: '#000',
    },
  },
});