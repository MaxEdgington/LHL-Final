import { createTheme } from '@mui/material';

// theme styles here
export const theme = createTheme({
  palette: {
    primary: {
      light: '#843333',
      main: '#660000',
      dark: '#470000',
      contrastText: '#fff',
    },
    secondary: {
      light: '#a3a2a9',
      main: '#d8c8b94',
      dark: '#626167',
      contrastText: '#000',
    },
  },
});